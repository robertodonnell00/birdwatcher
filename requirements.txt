picamera           # For capturing images with the Raspberry Pi Camera
RPi.GPIO           # To interact with the PIR motion sensor
requests           # For sending HTTP requests (e.g., to upload or use AI API)
azure-storage-blob # If you're using Azure Blob Storage
boto3              # If you're using AWS (S3 or Rekognition)
python-dotenv      # To manage environment variables (.env file for API keys)
azure-iot-device   # To send Metadata to hub
